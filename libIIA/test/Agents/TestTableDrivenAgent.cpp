#include "TableDrivenAgent.hpp"

#include "../TestProblem.h"

using namespace IIA::Test;
typedef IIA::Test::State::Integer Integer;

int main(int argc, char **argv) {
	/** @todo consertar teste */
	// std::map<Integer, const IIA::Action<Integer> *> lookup_table;
	// lookup_table[Integer(0)] = new Add(-1);
	// for(int i = 1; i < 10; ++i) {
	//	lookup_table[Integer(-i)] = new Add(-1);
	//	lookup_table[Integer(i)] = new Add(1);
	// }

	// Agents::TableDrivenAgent agent(lookup_table);
	// Problem::ZeroToFour problem;
	
	// Integer state = problem.initial_state;
	// while(!problem.is_goal(state)) state = agent.act(state);

	return 0;
}




